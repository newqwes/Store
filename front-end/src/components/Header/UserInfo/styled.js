import styled from 'styled-components';

const UserInfoContent = styled.div`
  display: flex;
  padding: 0 10px;

  button {
    margin-left: 20px;
    width: 100px;
  }
`;

export default UserInfoContent;
